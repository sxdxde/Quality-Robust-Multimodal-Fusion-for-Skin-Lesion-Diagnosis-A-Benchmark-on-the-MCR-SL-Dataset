\documentclass[conference]{IEEEtran}
\IEEEoverridecommandlockouts
\usepackage{cite}
\usepackage{amsmath,amssymb,amsfonts}
\usepackage{algorithmic}
\usepackage{graphicx}
\usepackage{textcomp}
\usepackage{xcolor}
\usepackage{tikz}
\usetikzlibrary{arrows.meta,positioning,fit,backgrounds,shapes.geometric}
\def\BibTeX{{\rm B\kern-.05em{\sc i\kern-.025em b}\kern-.08em
    T\kern-.1667em\lower.7ex\hbox{E}\kern-.125emX}}
\begin{document}

\title{Quality-Stratified Multimodal Fusion for Skin Lesion Malignancy Classification on MCR-SL}

\author{
\IEEEauthorblockN{1\textsuperscript{st} Given Name Surname}
\IEEEauthorblockA{\textit{Department Name} \\
\textit{Institution Name}\\
City, Country \\
email@example.com}
}

\maketitle

\begin{abstract}
Skin lesion diagnosis models have typically been trained on large dermoscopic archives that pair images with little or no structured patient metadata and with no measure of image quality at all. This paper has presented the first published benchmark on MCR-SL, a recently released multimodal skin lesion dataset comprising 240 lesions from 60 subjects, with paired clinical and dermoscopic images, twenty-two patient-level metadata fields, and per-image expert quality ratings. We have trained an EfficientNet-B0 image encoder combined with a metadata encoder under two fusion strategies, a concatenation-based baseline and a channel-gated fusion module conditioned on metadata, and have evaluated every configuration under subject-disjoint stratified five-fold cross-validation. The channel-gated fusion method has reached a balanced accuracy of 0.783 and an area under the receiver operating characteristic curve of 0.881. Beyond this ablation, we have conducted four robustness analyses that this dataset uniquely enables: stratification of performance by expert-rated image quality, a quality-aware auxiliary training objective, a comparison between histopathology-confirmed and panel-consensus-only lesions, and a post-hoc audit of learned metadata importance against the dataset's own reported statistical associations. A further sweep of seven follow-up interventions, tested individually and in four combinations (eleven configurations in total), has shown that sharpness-aware minimization combined with test-time augmentation has raised balanced accuracy to 0.822, close to the best verified published balanced accuracy for binary classification on the closest comparable multimodal dataset, 0.836, despite using substantially less training data, while quality-aware training, contrastive pretraining, an alternative optimizer configuration, and stacking every individually-promising intervention together have not improved results.
\end{abstract}

\begin{IEEEkeywords}
skin lesion classification, multimodal fusion, dermoscopy, image quality, metadata fusion, deep learning, medical imaging benchmark
\end{IEEEkeywords}

\section{Introduction}
Convolutional neural networks have reached dermatologist-level accuracy on dermoscopic image classification when trained on large, single-modality archives \cite{esteva2017}, and datasets such as HAM10000 \cite{ham10000} have supported this progress at scale. These archives, however, have carried little structured patient context beyond age, sex, and lesion site, even though a referring clinician has never made a diagnosis from an image alone. A smaller line of work has fused images with richer patient metadata, most notably through the Metadata Processing Block on the PAD-UFES-20 dataset \cite{metablock,paduf20}, and has shown that metadata fusion has improved balanced accuracy over image-only baselines. None of these datasets, to our knowledge, has recorded a per-image measure of diagnostic image quality, so no prior benchmark has been able to ask whether a classifier's errors have concentrated among the lesions that were hardest to photograph or judge in the first place.

MCR-SL \cite{mcrsl} has recently addressed this gap. It has paired 779 clinical and 1352 dermoscopic images across 240 lesions from 60 subjects with twenty-two subject-level metadata fields and, distinctively, with a one-to-ten quality rating that up to three dermatologists have assigned to the specific image used for each lesion's diagnosis. Since its publication, no prior work, to the best of our knowledge, has benchmarked a classification model on MCR-SL. This paper has addressed that gap and has used the dataset's unique quality-rating structure to ask a question that no prior skin lesion benchmark has been positioned to ask: does a multimodal fusion model's performance degrade on lesions whose diagnostic image was rated lower in quality, and can a model be trained to be more robust to this variation.

\subsection{Contributions}
This paper has made the following contributions.
\begin{itemize}
\item A first published benchmark on MCR-SL under a subject-disjoint stratified five-fold protocol, comparing an image-only baseline, a concatenation-based late-fusion baseline, and a channel-gated fusion method conditioned on patient metadata.
\item Four robustness analyses enabled specifically by MCR-SL's per-image expert quality ratings and partial histopathology confirmation: quality-stratified performance, a quality-aware auxiliary training objective, a histopathology-confirmed versus panel-consensus-only comparison, and a metadata-importance audit against the dataset's own reported statistical associations.
\item A sweep of seven further interventions, tested individually and in four combinations, including sharpness-aware minimization, test-time augmentation, multi-image evaluation averaging, focal loss, dermoscopy-specific preprocessing, and a supervised contrastive auxiliary objective, that has isolated which of these have actually improved performance at this dataset's scale and which have not.
\item A balanced accuracy of 0.822 after the best combination of interventions, within a narrow margin of the strongest published balanced accuracy on the closest comparable multimodal skin lesion dataset despite substantially less training data.
\end{itemize}

\section{Literature Review}
Image-only dermoscopic classification has been studied extensively since Esteva \emph{et al.} \cite{esteva2017} have shown that a single convolutional network can match dermatologist performance on a large, curated archive, with HAM10000 \cite{ham10000} subsequently becoming a standard benchmark for this line of work. These studies have generally treated the image as the sole input and have not incorporated structured clinical context.

Metadata fusion has emerged as a distinct thread. Pacheco and Krohling \cite{metablock} have proposed the Metadata Processing Block, an attention-based mechanism that has reweighted image features using patient metadata, and have evaluated it on PAD-UFES-20 \cite{paduf20}, a dataset of 2298 clinical images and 1641 lesions with twenty-six metadata fields collected via smartphone. This is the closest comparable dataset to MCR-SL in problem shape, real-world patient metadata paired with real-world images, and we have used it as our primary point of external comparison (Section~\ref{sec:comparison}). Our channel-gated fusion module has drawn its mechanism from Squeeze-and-Excitation networks \cite{se}, which have reweighted a convolutional feature map's channels using a signal derived from the feature map itself; our variant has derived that signal from patient metadata instead.

Cross-domain and few-shot medical imaging work has separately explored what happens when a training distribution and a deployment distribution have diverged, but has rarely operationalized image quality as an explicit, expert-rated variable available at training time. MCR-SL's per-image quality rating has made this operationalization possible for skin lesion classification for the first time, to our knowledge.

\subsection{Research Gap}
Existing multimodal skin lesion benchmarks have paired images with structured metadata but have not recorded an expert judgment of the diagnostic image's own quality, and existing quality-aware training methods in the broader medical imaging literature have not been evaluated on a dataset where quality-stratified ground truth is directly available at the per-lesion level rather than inferred from image statistics. This paper has addressed this gap by benchmarking a channel-gated fusion architecture on MCR-SL and by directly measuring, rather than approximating, the relationship between expert-rated image quality and classification performance.

\section{Proposed Methodology}
Fig.~\ref{fig:arch} summarizes the architecture. This paper has proposed a channel-gated fusion method for cross-modal skin lesion classification and has compared it against two baselines under an identical training and evaluation protocol.

\subsection{Architecture}
The image encoder is an ImageNet pretrained EfficientNet-B0 \cite{efficientnet,imagenet}, which has produced a 1280-dimensional pooled feature vector and the corresponding last convolutional feature map for every input image. The metadata encoder has processed sixteen categorical fields, each through its own learned embedding with an explicit unknown category reserved for missing values, and four numeric fields, age, height, weight, and lesion diameter, each z-score normalized using training-fold statistics only and paired with a missingness indicator bit; the concatenated representation has passed through a two-layer multilayer perceptron to a 128-dimensional metadata vector. Fields with a constant value across all subjects or with unusably sparse, free-text encodings have been excluded from the metadata encoder rather than imputed, consistent with the dataset's own stated policy of leaving missingness to end users.

Two fusion strategies have been compared, both ending in the same Linear-ReLU-Dropout projection to a 256-dimensional fused representation. The late-fusion baseline has concatenated the pooled image vector with the metadata vector before this projection. The channel-gated fusion method, the paper's main method, has instead passed the metadata vector through a linear layer and a sigmoid activation to produce a 1280-dimensional gate, and has multiplied this gate elementwise against the image encoder's convolutional feature map, channel by channel, before global average pooling and the same 256-dimensional projection; this has conditioned which visual channels the classifier has attended to on the patient's metadata rather than on the image alone. A binary head, a single linear layer on the 256-dimensional fused vector, has classified malignant versus non-malignant status using weighted binary cross-entropy, with class weights recomputed from each training fold's own malignant ratio. An auxiliary nine-class head has classified the unified diagnosis label at a downweighted loss contribution and has been reported as an exploratory result only, given that several of the nine classes have contained fewer than ten lesions. A quality-aware variant has attached a further regression head predicting the mean expert quality rating from the same fused representation, at a low loss weight, to test whether explicit quality supervision has improved robustness to quality variation (Section~\ref{sec:robustness}).

\begin{figure}[t]
\centering
\begin{tikzpicture}[
  box/.style={draw, rounded corners=2pt, align=center, minimum height=0.8cm, minimum width=2.3cm, font=\scriptsize, fill=blue!6},
  gbox/.style={draw, rounded corners=2pt, align=center, minimum height=0.95cm, minimum width=2.3cm, font=\scriptsize, fill=orange!12},
  hbox/.style={draw, rounded corners=2pt, align=center, minimum height=0.95cm, minimum width=2.3cm, font=\scriptsize, fill=green!10},
  outbox/.style={draw, rounded corners=2pt, align=center, minimum height=0.8cm, minimum width=2.3cm, font=\scriptsize, fill=red!8, thick},
  opnode/.style={draw, circle, font=\small, inner sep=1pt},
  arr/.style={-Stealth, thick},
  dimlbl/.style={font=\tiny, midway, sloped},
  pics/camera/.style={code={
    \draw[fill=blue!55,draw=blue!70!black] (-0.22,-0.13) rectangle (0.22,0.13);
    \draw[fill=blue!55,draw=blue!70!black] (0.09,0.13) rectangle (0.17,0.18);
    \draw[fill=white,draw=blue!70!black] (0,0) circle (0.09);
  }},
  pics/tableicon/.style={code={
    \draw[fill=orange!25,draw=orange!70!black] (-0.22,-0.16) rectangle (0.22,0.16);
    \foreach \x in {-0.22,-0.073,0.073}{\draw[draw=orange!70!black] (\x,-0.16) -- (\x,0.16);}
    \foreach \y in {-0.16,0,0.16}{\draw[draw=orange!70!black] (-0.22,\y) -- (0.22,\y);}
  }},
  pics/cnnstack/.style={code={
    \draw[fill=blue!20,draw=blue!60!black] (-0.26,-0.17) rectangle (0.02,0.17);
    \draw[fill=blue!40,draw=blue!60!black] (-0.13,-0.13) rectangle (0.13,0.13);
    \draw[fill=blue!65,draw=blue!60!black] (-0.02,-0.08) rectangle (0.26,0.08);
  }},
  pics/cube3d/.style={code={
    \draw[fill=blue!30,draw=blue!70!black] (-0.15,-0.15) rectangle (0.15,0.15);
    \draw[fill=blue!50,draw=blue!70!black] (-0.15,0.15) -- (-0.06,0.24) -- (0.24,0.24) -- (0.15,0.15) -- cycle;
    \draw[fill=blue!40,draw=blue!70!black] (0.15,-0.15) -- (0.24,-0.06) -- (0.24,0.24) -- (0.15,0.15) -- cycle;
  }},
  pics/mlpnet/.style={code={
    \foreach \y in {-0.14,0,0.14}{\foreach \z in {-0.14,0,0.14}{\draw[gray!70,thin] (-0.15,\y) -- (0.15,\z);}}
    \foreach \y in {-0.14,0,0.14}{\filldraw[orange!80!black] (-0.15,\y) circle (0.03);\filldraw[orange!80!black] (0.15,\y) circle (0.03);}
  }},
  pics/gatebar/.style={code={
    \foreach \i/\c in {0/25,1/85,2/45,3/95,4/15,5/65,6/35,7/75}{
      \draw[fill=orange!\c,draw=orange!70!black,line width=0.15pt] (\i*0.075,0) rectangle ++(0.07,0.16);}
  }},
  pics/funnelicon/.style={code={
    \draw[fill=blue!25,draw=blue!70!black] (-0.2,0.18) -- (0.2,0.18) -- (0.07,-0.1) -- (-0.07,-0.1) -- cycle;
  }},
  pics/binaryicon/.style={code={
    \draw[fill=green!55,draw=green!40!black] (0,0) circle (0.13);
    \fill[red!65] (0,0) -- (90:0.13) arc (90:270:0.13) -- cycle;
  }},
  pics/barchart9/.style={code={
    \foreach \i/\h in {0/0.04,1/0.11,2/0.07,3/0.16,4/0.05,5/0.13,6/0.09,7/0.15,8/0.08}{
      \draw[fill=green!55!black,draw=none] (\i*0.045,0) rectangle ++(0.035,\h);}
  }},
  pics/gaugeicon/.style={code={
    \draw[thick,green!40!black] (-0.16,0) arc (180:0:0.16);
    \draw[thick,-{Stealth[length=1.3mm]},green!40!black] (0,0) -- (40:0.14);
  }},
  pics/checkicon/.style={code={
    \draw[green!45!black, line width=1.3pt] (-0.11,0) -- (-0.02,-0.09) -- (0.13,0.11);
  }}
]

\pic at (-1.55,8.0) {camera};
\node (img) at (0,8.0) {\includegraphics[width=1.7cm]{figures/examples/L0189_I_D00895.jpg}};
\node[draw, thick, fit=(img), inner sep=1pt] {};
\node[font=\tiny, below=0.02cm of img] {Dermoscopic image (example)};
\node[font=\tiny] at (0,8.9) {input: $3\!\times\!224\!\times\!224$};

\pic at (3.75,8.0) {tableicon};
\node[box, minimum width=2.3cm] (meta) at (5.2,8.0) {Metadata (example):\\age 70, male, face,\\referral: BCC};
\node[font=\tiny] at (5.2,8.65) {22 fields (16 cat.\ + 4 num.)};

\pic at (-1.55,5.9) {cnnstack};
\node[box] (enc) at (0,5.9) {EfficientNet-B0\\encoder};
\pic at (3.75,5.9) {mlpnet};
\node[box] (menc) at (5.2,5.9) {Categorical embed.\\+ numeric MLP};

\pic at (0.85,4.85) {cube3d};
\node[font=\tiny] at (0.85,4.55) {$1280\!\times\!7\!\times\!7$};
\pic at (3.75,4.3) {gatebar};
\node[gbox] (gate) at (5.2,4.3) {$\sigma($Linear$)$\\channel gate $\in[0,1]^{1280}$};

\node[opnode] (mult) at (2.6,2.7) {$\otimes$};
\pic at (1.75,1.1) {funnelicon};
\node[box] (pool) at (2.6,1.1) {Gated global pool $\to 1280$-d};

\pic at (1.05,-0.15) {mlpnet};
\node[box] (proj) at (2.6,-0.15) {Linear+ReLU+Dropout\\$\to 256$-d (fused)};

\pic at (-1.05,-1.6) {binaryicon};
\node[hbox] (bin) at (0,-1.6) {Binary head\\(malignant)};
\pic at (1.55,-1.6) {barchart9};
\node[hbox] (aux) at (2.6,-1.6) {Aux.\ head\\(9-class)};
\pic at (4.15,-1.6) {gaugeicon};
\node[hbox] (qual) at (5.2,-1.6) {Quality head\\(optional)};

\pic at (-1.05,-3.3) {checkicon};
\node[outbox] (out) at (0,-3.3) {Output (example):\\$P$(malignant){=}1.00\\$\rightarrow$ Malignant (correct)};

\draw[arr] (img) -- (enc) node[dimlbl,above] {};
\draw[arr] (meta) -- (menc);
\draw[arr] (menc) -- (gate) node[dimlbl,above] {128-d};
\draw[arr] (enc.south) -- ++(0,-1.1) -| (mult.north);
\draw[arr] (gate.west) -| (mult.north) node[dimlbl,above,pos=0.75] {1280-d gate};
\draw[arr] (mult) -- (pool);
\draw[arr] (pool) -- (proj);
\draw[arr] (proj.south) -- ++(0,-0.55) -| (bin.north);
\draw[arr] (proj.south) -- (aux.north);
\draw[arr] (proj.south) -- ++(0,-0.55) -| (qual.north);
\draw[arr] (bin) -- (out);
\end{tikzpicture}
\caption{Channel-gated fusion architecture, with tensor shapes and one real example flowing through: lesion L0189, a histopathology-confirmed basal cell carcinoma, one of the model's highest-confidence correct predictions ($\otimes$: elementwise multiplication of the metadata-derived gate with the convolutional feature map, per channel, before pooling). The late-fusion baseline shares the same final Linear+ReLU+Dropout projection, applied instead to concat(pooled image, metadata) directly. Full example gallery in Fig.~\ref{fig:examples}.}
\label{fig:arch}
\end{figure}

\subsection{Training and Evaluation Protocol}
Every configuration has been evaluated under subject-disjoint stratified five-fold cross-validation, assigning each subject's lesions to exactly one fold so that no subject or near-duplicate lesion image has appeared in both a training and an evaluation fold. Folds have been balanced by greedily assigning subjects to minimize the difference in malignant lesion count across folds. For each of the five rotations, one fold has been held out as the reported test fold and a second, disjoint fold has been held out purely for checkpoint selection by validation balanced accuracy; the test fold has never influenced model selection, and no hyperparameter has been tuned against final test-fold results.

We have additionally evaluated seven follow-up interventions on the channel-gated method, each trained and evaluated under the identical protocol: focal loss \cite{focal} in place of plain weighted cross-entropy, dermoscopy-specific preprocessing combining hair removal and gray-world color normalization, a supervised contrastive auxiliary loss on the fused representation \cite{supcon}, an alternative optimizer using decoupled weight decay with a cosine learning-rate schedule and a reduced backbone learning rate \cite{adamw}, sharpness-aware minimization \cite{sam} wrapping the same optimizer, test-time augmentation via horizontal flip averaging, and averaging predictions across every dermoscopic image of a lesion rather than the single image used for its original diagnosis. The last two are evaluation-time only and have required no retraining; we have additionally evaluated three combinations of sharpness-aware minimization with these two evaluation-time interventions, plus a fourth combination stacking preprocessing, focal loss, sharpness-aware minimization, and test-time augmentation together, for eleven configurations in total.

\section{Experimental Results}

\subsection{Dataset}
\label{sec:dataset}
Table~\ref{tab:dataset} summarizes MCR-SL \cite{mcrsl} as used in this study. Six lesions carry an undetermined malignancy label and have been excluded from the binary classification task, leaving 234 usable lesions; five further lesions carry an undetermined unified diagnosis and have been additionally excluded from the nine-class auxiliary task only. Twenty-one lesion identifiers present in the image index have no corresponding row in the lesion table and no file on disk; these have been dropped via an inner join and logged rather than silently discarded. The 238 lesions with full expert quality coverage (Table~\ref{tab:dataset}) overlap the 234 binary-usable lesions almost completely; Section~\ref{sec:robustness} traces the small further reduction, to 231, used in the quality-stratified analyses.

\begin{table}[t]
\caption{MCR-SL dataset summary, as verified against the released files}
\label{tab:dataset}
\centering
\small
\begin{tabular}{|l|l|}
\hline
\textbf{Property} & \textbf{Value} \\
\hline
Subjects & 60 \\
Lesions (total / usable for binary task) & 240 / 234 \\
Malignant / non-malignant / undetermined & 42 / 192 / 6 \\
Clinical images (usable) & 779 \\
Dermoscopic images (usable) & 1352 \\
Histopathology-confirmed lesions & 28 \\
Metadata fields used (categorical / numeric) & 16 / 4 \\
Lesions with full expert quality coverage & 238 \\
Unified diagnosis classes & 9 \\
\hline
\end{tabular}
\par\vspace{2pt}\footnotesize ``Usable'' images are those with a matching lesion record and an existing file on disk (2394 raw image-index rows include 263 with neither, dropped via an inner join, see Section~\ref{sec:dataset}); the resulting counts match the dataset paper's own reported totals exactly.
\end{table}

\subsection{Example Inputs and Outputs}
Fig.~\ref{fig:examples} shows the model's actual input and output for four real lesions: the two most confident correct malignant calls and the two most confident correct non-malignant calls in the evaluation set. These are best-case, not representative, examples, intended to illustrate the input/output format concretely; the paper's actual, unfiltered performance, including its errors, is reported in full in Tables~\ref{tab:ablation}--\ref{tab:comparison} and is not restated here.

\begin{figure}[t]
\centering
\includegraphics[width=\linewidth]{figures/example_gallery.png}
\caption{Highest-confidence correct predictions, channel-gated method: two malignant (basal cell carcinoma, both histopathology-confirmed) and two non-malignant lesions. Border color denotes a correct prediction.}
\label{fig:examples}
\end{figure}

\subsection{Evaluation Metrics}
We have reported accuracy, balanced accuracy, macro-averaged F1, sensitivity on the malignant class, specificity, and area under the receiver operating characteristic curve, AUROC, at every shot of evaluation, aggregated across the five test folds as mean and standard deviation. Balanced accuracy has been used for checkpoint selection and as the primary comparison metric, since the malignant class has represented a minority of usable lesions.

\subsection{Core Ablation Results}
Table~\ref{tab:ablation} reports the core ablation matrix at three-decimal precision, since several values are close enough that a two-decimal table would not let a reader verify ordinal claims between them. The channel-gated fusion method has reached the highest macro-F1 among the three architectures, but has not dominated every metric: the image-only baseline has reached a materially higher sensitivity (0.697 vs.\ 0.672) and AUROC (0.895 vs.\ 0.881), and a balanced accuracy that is, at this precision, effectively tied with channel-gated's (0.784 vs.\ 0.783, a 0.001 difference against a standard deviation of 0.07--0.09). This result has been reported as such rather than framed as a clean win for the proposed fusion method.

The quality-aware variant, which has added an auxiliary head predicting expert image-quality rating, has underperformed the plain channel-gated method on five of the six metrics: balanced accuracy 0.740 vs.\ 0.783, sensitivity 0.578 vs.\ 0.672 (a 0.094 drop), and AUROC 0.879 vs.\ 0.881. The exception is specificity, 0.902 vs.\ 0.895, a small real improvement. Together these describe a model that has become more conservative under quality-aware training, trading a large sensitivity loss for a small specificity gain, arguably the worse direction for a cancer-screening task where a missed malignancy is costlier than a false alarm. Section~\ref{sec:robustness} has examined this result directly.

\begin{table}[t]
\caption{Core ablation matrix, mean $\pm$ std over five folds}
\label{tab:ablation}
\centering
\scriptsize
\setlength{\tabcolsep}{2pt}
\begin{tabular}{|l|c|c|c|}
\hline
\textbf{Metric} & \textbf{Image-only} & \textbf{Late fusion} & \textbf{Channel-gated} \\
\hline
Accuracy & 0.813 $\pm$ 0.020 & 0.831 $\pm$ 0.052 & 0.833 $\pm$ 0.070 \\
Balanced acc. & 0.784 $\pm$ 0.073 & 0.768 $\pm$ 0.090 & 0.783 $\pm$ 0.085 \\
Macro F1 & 0.733 $\pm$ 0.038 & 0.743 $\pm$ 0.051 & 0.757 $\pm$ 0.073 \\
Sensitivity & 0.697 $\pm$ 0.187 & 0.644 $\pm$ 0.191 & 0.672 $\pm$ 0.159 \\
Specificity & 0.872 $\pm$ 0.061 & 0.891 $\pm$ 0.021 & 0.895 $\pm$ 0.048 \\
AUROC & 0.895 $\pm$ 0.053 & 0.851 $\pm$ 0.080 & 0.881 $\pm$ 0.065 \\
\hline
\end{tabular}
\par\vspace{2pt}\footnotesize Channel-gated, quality-aware variant: accuracy 0.812 $\pm$ 0.068, balanced accuracy 0.740 $\pm$ 0.065, macro F1 0.715 $\pm$ 0.065, sensitivity 0.578 $\pm$ 0.177, specificity 0.902 $\pm$ 0.073, AUROC 0.879 $\pm$ 0.041.
\end{table}

\subsection{Follow-Up Intervention Sweep}
Table~\ref{tab:extended} reports eleven follow-up configurations against the channel-gated baseline: seven distinct interventions plus four stacked combinations of them. Sharpness-aware minimization combined with test-time augmentation has reached the highest balanced accuracy of any configuration, 0.822, and has also raised specificity to 0.925. Sharpness-aware minimization alone has already outperformed every other single-model configuration except the two evaluation-time-only interventions, consistent with the premise that seeking a flatter loss minimum has benefited a small, noisy training set. Notably, sharpness-aware minimization's two forward-backward passes per step have not doubled wall-clock training time in this setting, which has suggested the training pipeline was data-loading bound rather than backward-pass bound at this model size.

Multi-image evaluation averaging has been the strongest single evaluation-time-only intervention, requiring no retraining, but has not stacked additively with sharpness-aware minimization: combining sharpness-aware minimization with test-time augmentation and multi-image averaging together has reduced balanced accuracy to 0.810 relative to sharpness-aware minimization and test-time augmentation alone. The dataset-wide highest AUROC, 0.917, has instead come from sharpness-aware minimization combined with multi-image averaging without test-time augmentation, a further illustration that these evaluation-time interventions do not combine predictably. Focal loss and dermoscopy preprocessing have traded off in opposite, interpretable directions: focal loss has raised specificity to 0.923 at sensitivity's expense, while preprocessing has reached the highest sensitivity of any configuration, 0.736, plausibly because hair and lighting artifacts removed by preprocessing have obscured exactly the visual features relevant to malignant lesions. Contrastive pretraining, the alternative optimizer configuration, and quality-aware training have each underperformed the plain baseline, a convergent negative result across three independent interventions suggesting that, at this dataset's scale, added training objective complexity has not reliably helped. Stacking every individually-promising intervention together (preprocessing, focal loss, sharpness-aware minimization, test-time augmentation) has underperformed both the plain baseline and SAM+TTA, 0.759 balanced accuracy against 0.783 and 0.822, with markedly higher fold-to-fold variance than any other configuration; a plausible cause is that focal loss and preprocessing pull the decision boundary in opposite directions (toward specificity and sensitivity respectively), a tension the additional interventions have not resolved.

\begin{table}[t]
\caption{Follow-up configurations vs.\ the channel-gated baseline, mean over five folds. Balanced-accuracy std for these configurations ranged 0.04--0.10, comparable to Table~\ref{tab:ablation}}
\label{tab:extended}
\centering
\scriptsize
\setlength{\tabcolsep}{2pt}
\begin{tabular}{|l|c|c|c|c|c|}
\hline
\textbf{Configuration} & \textbf{Acc.} & \textbf{Bal.\ acc.} & \textbf{Sens.} & \textbf{Spec.} & \textbf{AUROC} \\
\hline
Focal loss ($\gamma$=2) & 0.858 & 0.809 & 0.694 & 0.923 & 0.877 \\
Dermoscopy preprocessing & 0.828 & 0.793 & 0.736 & 0.851 & 0.875 \\
Contrastive auxiliary loss & 0.812 & 0.770 & 0.669 & 0.872 & 0.873 \\
AdamW, cosine, discrim.\ LR & 0.822 & 0.757 & 0.661 & 0.853 & 0.845 \\
Test-time augmentation (TTA) & 0.841 & 0.788 & 0.672 & 0.904 & 0.897 \\
Multi-image averaging & 0.854 & 0.813 & 0.719 & 0.907 & 0.906 \\
Sharpness-aware min.\ (SAM) & 0.858 & 0.810 & 0.694 & 0.927 & 0.904 \\
SAM + multi-image & 0.851 & 0.800 & 0.672 & 0.928 & \textbf{0.917} \\
\textbf{SAM + TTA} & \textbf{0.861} & \textbf{0.822} & \textbf{0.719} & 0.925 & 0.908 \\
SAM + TTA + multi-image & 0.856 & 0.810 & 0.694 & 0.925 & 0.915 \\
Preprocessing+focal+SAM+TTA & 0.818 & 0.759 & 0.622 & 0.895 & 0.893 \\
\hline
\end{tabular}
\end{table}

\subsection{Robustness Analyses}
\label{sec:robustness}
All four robustness analyses have used the channel-gated method's out-of-fold predictions, the paper's designated main method, so that this section remains directly tied to the core ablation result in Table~\ref{tab:ablation} rather than to the best follow-up configuration. Of the 234 lesions usable for the binary task, 231 have received an out-of-fold prediction; the remaining three lacked a usable image in their assigned test fold and were excluded from evaluation rather than imputed or dropped silently. All four analyses below operate on these 231 lesions, and every one of them also carries a valid quality rating, so no further lesions are lost between the quality-stratified and histopathology comparisons.

\textbf{Quality-stratified performance.} Lesions have been grouped into terciles by mean expert quality rating across the up to three experts who rated each lesion's diagnostic image, one expert's ratings having been entirely lost to a recorded technical error and excluded. Fig.~\ref{fig:tercile} and Table~\ref{tab:tercile} report accuracy and malignant sensitivity per tercile for all 231 evaluated lesions (Table~\ref{tab:tercile} also includes the quality-aware variant's tercile breakdown, discussed below). The tercile sizes are uneven (85, 93, 53) rather than equal thirds of 77, because \texttt{mean\_image\_rating} is an average over up to three integer 1--10 ratings and takes only a limited set of possible values, so tied values cluster at the tercile boundaries; the high tercile in particular is the smallest, and its sensitivity of 0.500 should be read as a more volatile estimate than the other two given its smaller N. The pattern has not been monotonic, and the Spearman correlation between rating and prediction error has been $-0.093$ ($p=0.158$), not significant at this sample size; we have reported this as no strong quality-performance relationship detected at 231 lesions, neither confirming nor ruling out an effect.

\begin{figure}[t]
\centering
\includegraphics[width=0.85\linewidth]{figures/quality_tercile_clean.png}
\caption{Accuracy by expert-rated image-quality tercile, channel-gated method. Tercile sizes are uneven (Table~\ref{tab:tercile}: 85, 93, 53 of $N=231$) due to tied values in the underlying rating.}
\label{fig:tercile}
\end{figure}

\begin{table}[t]
\caption{Performance by image-quality tercile, channel-gated method, without and with quality-aware training (QA). Accuracy gap, high $-$ low tercile: 0.091 without QA and 0.101 with QA, both from unrounded values; subtracting the rounded figures shown here gives 0.092 for the without-QA row instead of 0.091, a rounding artifact, not a data change}
\label{tab:tercile}
\centering
\scriptsize
\setlength{\tabcolsep}{3pt}
\begin{tabular}{|l|c|c|c|c|c|}
\hline
\textbf{Tercile} & \textbf{N} & \textbf{Acc.} & \textbf{Acc.\ (QA)} & \textbf{Sens.} & \textbf{Sens.\ (QA)} \\
\hline
Low & 85 & 0.776 & 0.729 & 0.667 & 0.524 \\
Mid & 93 & 0.914 & 0.903 & 0.733 & 0.667 \\
High & 53 & 0.868 & 0.830 & 0.500 & 0.500 \\
\hline
\end{tabular}
\end{table}

\textbf{Quality-aware training.} The auxiliary quality-regression head, intended to make the model explicitly robust to quality variation, has instead widened the accuracy gap between the lowest and highest quality terciles, from 0.091 without quality awareness to 0.101 with it (Table~\ref{tab:tercile}'s QA columns). This is a direct negative result for the intervention as implemented, reported without adjustment.

\textbf{Histopathology-confirmed versus panel-consensus.} Table~\ref{tab:histo} compares the 28 histopathology-confirmed lesions against the 203 panel-consensus-only lesions. The model has performed worse on the histopathology-confirmed subset, plausibly because histopathology is more often ordered for clinically ambiguous or suspicious lesions in real practice. This comparison has been treated as qualitative only, given that 28 lesions is too small a sample for a reliable confidence interval.

\begin{table}[t]
\caption{Histopathology-confirmed vs.\ panel-consensus-only lesions, channel-gated method}
\label{tab:histo}
\centering
\small
\begin{tabular}{|l|c|c|}
\hline
\textbf{Group} & \textbf{N} & \textbf{Accuracy} \\
\hline
Histopathology-confirmed & 28 & 0.750 \\
Panel-consensus-only & 203 & 0.867 \\
\hline
\end{tabular}
\end{table}

\textbf{Metadata importance.} We have measured each metadata field's importance by ablation, replacing that field with its reserved unknown or missing value and recording the mean absolute change in the binary logit across the full evaluation set, chosen over a gradient-times-input measure because the latter has structurally favored multi-dimensional categorical embeddings over single-scalar numeric fields regardless of true importance. Table~\ref{tab:metaimp} reports the highest and lowest ranked fields against the four fields the MCR-SL dataset paper itself has reported as significantly associated with malignancy. Referral diagnosis, sex, and location group have ranked in the upper half of the importance ordering, consistent with the dataset paper, but all four numeric fields, including lesion diameter, have ranked lowest. We have attributed this to an architectural capacity imbalance, a twelve-dimensional learned embedding per categorical field against a single normalized scalar per numeric field, rather than to diameter being uninformative, and have flagged it as a limitation rather than a finding about the field itself.

\begin{table}[t]
\caption{Metadata field importance (mean absolute logit change under ablation). * denotes a field the dataset paper has reported as significantly associated with malignancy}
\label{tab:metaimp}
\centering
\small
\begin{tabular}{|l|c|}
\hline
\textbf{Field (highest ranked)} & \textbf{Importance} \\
\hline
Organ transplant history & 0.046 \\
Lesion status when captured & 0.041 \\
Referral diagnosis* & 0.024 \\
Sex* & 0.015 \\
Location group* & 0.011 \\
\hline
\textbf{Field (lowest ranked)} & \textbf{Importance} \\
\hline
Age & 0.0011 \\
Weight & 0.0010 \\
Lesion diameter* & 0.0010 \\
Height & 0.0007 \\
\hline
\end{tabular}
\end{table}

\subsection{Comparison with the Literature}
\label{sec:comparison}
Table~\ref{tab:comparison} situates the results against the closest published work under a matching binary task. PAD-UFES-20 \cite{paduf20} is the nearest comparator dataset in problem shape, real-world clinical images paired with real patient metadata; six-class balanced accuracy figures have been reported for it by several methods, including the Metadata Processing Block \cite{metablock}, in the range 0.765--0.842, but a six-class metric is not a valid numeric comparator for our binary malignant versus non-malignant task despite sharing the same metric name, and we have not used it as one. The binary cancer-versus-non-cancer PAD-UFES-20 result we have been able to verify directly is Uliana and Krohling's \cite{diffmic}, 0.836 balanced accuracy from a diffusion-based classifier evaluated by five-fold cross-validation over 2298 images, a broadly similar protocol to ours though at the image level rather than our lesion level. This paper's best follow-up configuration, sharpness-aware minimization with test-time augmentation, has reached 0.822, close to but below this verified binary benchmark. That same paper has further attributed an appreciably higher figure, approximately 0.88, to an earlier ResNet-50 configuration; we report this for transparency but have not independently verified it against its original source, so we have not treated it as a confirmed benchmark. HAM10000 \cite{ham10000} headline results have reached higher raw accuracy, but this has reflected a dermoscopy-only task, no metadata, and roughly forty times more training images, and has not been treated as a directly comparable figure in this paper.

\begin{table}[t]
\caption{Comparison with the closest verified published binary-task result. PAD-UFES-20's own six-class figures (0.765--0.842) are omitted as not comparable to a binary task}
\label{tab:comparison}
\centering
\footnotesize
\setlength{\tabcolsep}{3pt}
\begin{tabular}{|l|c|c|c|}
\hline
\textbf{Source} & \textbf{Task} & \textbf{N} & \textbf{Bal.\ acc.} \\
\hline
This paper (channel-gated) & binary & 234 lesions & 0.783 \\
This paper (SAM + TTA) & binary & 234 lesions & \textbf{0.822} \\
DiffMIC, PAD-UFES-20 \cite{diffmic} & binary & 2298 images & 0.836 \\
\hline
\end{tabular}
\end{table}

Given that MCR-SL has had no prior published benchmark, this paper's result is, technically, the current best on this dataset by default; we have deliberately avoided framing this as a state-of-the-art claim in the conventional sense, since there has been no prior published number to surpass, and have instead emphasized the robustness analyses in Section~\ref{sec:robustness} as the paper's principal contribution.

\section{Conclusion and Future Work}
This paper has presented the first published benchmark on MCR-SL, evaluating an image-only baseline, a late-fusion baseline, and a channel-gated fusion method under subject-disjoint stratified five-fold cross-validation (0.783 balanced accuracy for the main method), plus seven follow-up interventions tested individually and in four combinations. Sharpness-aware minimization with test-time augmentation has reliably improved results, to 0.822; contrastive pretraining, an alternative optimizer, quality-aware training, and stacking every individually-promising intervention together have not, and these interventions have not stacked additively with one another. Four robustness analyses enabled by MCR-SL's unique per-image quality ratings and partial histopathology confirmation have found no significant relationship between expert-rated image quality and error at this sample size, a quality-aware objective that has widened rather than narrowed the quality-tercile gap, weaker performance on histopathology-confirmed than panel-consensus lesions, and metadata importance partially, but not fully, aligned with the dataset paper's own statistical associations.

This study has been limited by MCR-SL's small size, 234 usable lesions from a single institution, and by its ground truth, only 28 lesions histopathology-confirmed, the remainder resting on dermatologist panel consensus; these limitations bound how far the reported numbers should be generalized. Future work should extend the metadata encoder to give numeric fields representational capacity comparable to categorical embeddings before re-running the importance audit, evaluate a text-templated encoding of the same metadata against the tabular encoder used here, and revisit quality-aware training with a stronger or differently weighted quality-prediction objective given its negative result here.

\begin{thebibliography}{00}
\bibitem{mcrsl} M. Castro-Fernandez \emph{et al.}, ``MCR-SL: A multimodal, context-rich skin lesion dataset for skin cancer diagnosis,'' \emph{Data}, vol. 10, no. 10, art. 166, 2025.
\bibitem{esteva2017} A. Esteva, B. Kuprel, R. A. Novoa, J. Ko, S. M. Swetter, H. M. Blau, and S. Thrun, ``Dermatologist-level classification of skin cancer with deep neural networks,'' \emph{Nature}, vol. 542, pp. 115--118, 2017.
\bibitem{ham10000} P. Tschandl, C. Rosendahl, and H. Kittler, ``The HAM10000 dataset, a large collection of multi-source dermatoscopic images of common pigmented skin lesions,'' \emph{Sci. Data}, vol. 5, art. 180161, 2018.
\bibitem{metablock} A. G. C. Pacheco and R. A. Krohling, ``An attention-based mechanism to combine images and metadata in deep learning models applied to skin cancer classification,'' \emph{IEEE J. Biomed. Health Inform.}, vol. 25, no. 9, pp. 3554--3563, 2021.
\bibitem{paduf20} A. G. C. Pacheco \emph{et al.}, ``PAD-UFES-20: A skin lesion dataset composed of patient data and clinical images collected from smartphones,'' \emph{Data in Brief}, vol. 32, art. 106221, 2020.
\bibitem{se} J. Hu, L. Shen, and G. Sun, ``Squeeze-and-excitation networks,'' in \emph{Proc. IEEE Conf. Computer Vision and Pattern Recognition (CVPR)}, 2018.
\bibitem{efficientnet} M. Tan and Q. V. Le, ``EfficientNet: Rethinking model scaling for convolutional neural networks,'' in \emph{Proc. Int. Conf. Machine Learning (ICML)}, 2019, pp. 6105--6114.
\bibitem{imagenet} J. Deng, W. Dong, R. Socher, L.-J. Li, K. Li, and L. Fei-Fei, ``ImageNet: A large-scale hierarchical image database,'' in \emph{Proc. IEEE Conf. Computer Vision and Pattern Recognition (CVPR)}, 2009, pp. 248--255.
\bibitem{sam} P. Foret, A. Kleiner, H. Mobahi, and B. Neyshabur, ``Sharpness-aware minimization for efficiently improving generalization,'' in \emph{Proc. Int. Conf. Learning Representations (ICLR)}, 2021.
\bibitem{adamw} I. Loshchilov and F. Hutter, ``Decoupled weight decay regularization,'' in \emph{Proc. Int. Conf. Learning Representations (ICLR)}, 2019.
\bibitem{supcon} P. Khosla \emph{et al.}, ``Supervised contrastive learning,'' in \emph{Advances in Neural Information Processing Systems (NeurIPS)}, vol. 33, 2020, pp. 18661--18673.
\bibitem{focal} T.-Y. Lin, P. Goyal, R. Girshick, K. He, and P. Doll\'ar, ``Focal loss for dense object detection,'' in \emph{Proc. IEEE Int. Conf. Computer Vision (ICCV)}, 2017, pp. 2980--2988.
\bibitem{diffmic} J. J. M. Uliana and R. A. Krohling, ``Diffusion models applied to skin and oral cancer classification,'' 2025, arXiv:2504.00026. [Online]. Available: https://arxiv.org/abs/2504.00026
\end{thebibliography}

\end{document}
